import React from 'react';

type CellProps = {
    width: number;
    height: number;
    text: string;
}

        // <div style={{
        // width: `${width}px`,
        // height: `${height}px`
        // }}>
        // {text}
        // </div>
  
const Cell = ( {width, height, text}: CellProps) => {
    return (
        "something"

    )
}
export default Cell;